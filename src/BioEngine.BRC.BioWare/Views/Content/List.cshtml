@model BioEngine.Core.Site.Model.ListModel<BioEngine.Core.Entities.ContentItem, int>

<div>
    @foreach (var item in Model.Items)
    {
        <div>
            <h1>@item.Title</h1>
        </div>
    }
</div>