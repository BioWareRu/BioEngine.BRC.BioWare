@using BioEngine.BRC.BioWare.ViewComponents
@model BioEngine.Core.Entities.Menu
@if (Model != null)
{
    await Html.RenderPartialAsync("Menu", new MenuLevel {Items = Model.Items, Level = 0});
}
else
{
    <div>MENU</div>
}