@model BioEngine.Core.Site.Model.BlockViewModel<BioEngine.Core.Entities.Blocks.CutBlock, BioEngine.Core.Entities.Blocks.CutBlockData>

<a href="@Model.Post.PublicUrl" class="cutlink">
    @{
        if (string.IsNullOrEmpty(Model.Block.Data.ButtonText))
        {
            <text>
                "Читать дальше"
            </text>
        }
        else
        {
            <text>
                @Model.Block.Data.ButtonText
            </text>
        }
    }
</a>