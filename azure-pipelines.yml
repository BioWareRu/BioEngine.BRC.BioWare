pool:
    vmImage: 'Ubuntu 16.04'

variables:
    Version: '3.0.$(Build.BuildId)'
    ImageName: biowareru/bioengine-brc-bw:$(Version)
    
steps:
    - task: UseDotNet@2
      displayName: 'Install .Net Core 3.0'
      inputs:
          packageType: sdk
          version: 3.0.100-preview6-012264
          includePreviewVersions: true

    - task: UseNode@1
      displayName: 'install node'
      inputs:
          version: '11.x'
          checkLatest: true

    - task: DotNetCoreCLI@2
      displayName: 'Restore packages'
      inputs:
          command: restore
          projects: '**/*.csproj'
          feedsToUse: 'select'
          verbosityRestore: 'minimal'
          vstsFeed: 'BioEngine'

    - task: Npm@1
      displayName: 'Install npm packages'
      inputs:
          command: 'custom'
          customCommand: 'ci'
          workingDir: 'src/BioEngine.BRC.BioWare'

    - task: Npm@1
      displayName: 'Build frontend'
      inputs:
          command: 'custom'
          customCommand: 'run build-prod'
          workingDir: 'src/BioEngine.BRC.BioWare'

    - task: DotNetCoreCLI@2
      displayName: 'Publish project'
      inputs:
          command: custom
          custom: publish
          publishWebProjects: false
          zipAfterPublish: false
          modifyOutputPath: false
          arguments: '--no-restore -c Release --output ./publish'
          projects: '**/*.csproj'

    - task: Docker@2
      displayName: 'Build and push image'
      inputs:
          containerRegistry: 'BioWareRU DockerHub'
          repository: 'biowareru/bioengine-brc-bw'
          command: 'buildAndPush'
          Dockerfile: 'src/BioEngine.BRC.BioWare/Dockerfile'
          buildContext: './publish'
          tags: '$(Version)'
    
