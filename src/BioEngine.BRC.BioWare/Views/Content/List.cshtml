@using BioEngine.Core.Entities
@using BioEngine.Core.Site.Model
@model BioEngine.Core.Site.Model.ListViewModel<BioEngine.Core.Entities.ContentItem, int>
@{
    Layout = "~/Views/_Main.cshtml";
}
<div class="article-list">
    @foreach (var item in Model.Items)
    {
        await Html.RenderPartialAsync("Content/Show", new ContentViewModel<ContentItem>(item, ContentViewMode.Short));
    }
    <vc:pager page="@Model.Page" total-items="@Model.TotalItems" items-per-page="@Model.ItemsPerPage"></vc:pager>
</div>